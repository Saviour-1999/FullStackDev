spring:
 datasource:
  driverClassName: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://localhost:3306/bitter
  username: root
  password: Kolkata@1999
 jpa:
  hibernate.ddl-auto: update
  generate-ddl: true
  show-sql: true
  properties.hibernate.format_sql : trues
  use_sql_comments : true
# security:
 #  user:
 #    name:root
  #   password:root
  
jwt:
 get.token.uri=/authenticate
 signing.key.secret=mySecret
 refresh.token.uri=/refresh
 http.request.header=Authorization
 token.expiration.in.seconds=604800